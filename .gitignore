# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Node (only if tooling is added later)
node_modules/
.wrangler/

# Local env
.env
.env.*

# Local LLM dev key (Cloudflare wrangler) — NEVER commit
.dev.vars
